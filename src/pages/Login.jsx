const Login = () => {
  return (
    <section
      className="padding dark:bg-gray-800"
      onClick={() => setShowMenu(() => false)}
    >
      <div className="max-w-6xl h-screen mx-auto grid place-items-center">
        <h2 className="text-2xl dark:text-slate-300">
          sorry, this function is not available yet... :(
        </h2>
      </div>
    </section>
  );
};
export default Login;
